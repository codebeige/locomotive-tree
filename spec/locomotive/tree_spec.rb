require 'spec_helper'

describe Locomotive::Tree do
  it 'has a version number' do
    expect(Locomotive::Tree::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
